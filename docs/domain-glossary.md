# AI Agent Domain Glossary

This glossary defines the preferred English and Japanese terms for AI-agent-related documents. Use the preferred terms consistently. Terms listed in `Avoid` are not necessarily forbidden, but should not be used as the primary wording when the preferred term applies.

| English | Japanese | description | Avoid |
|---|---|---|---|
| <a id="agent"></a>Agent | エージェント | 指示、ツール、必要に応じたハンドオフやガードレールを持ち、目的達成のために処理を進めるAI実行主体。単なるLLM呼び出しではなく、作業を進める単位を指す。 | AIエージェント, AI agent, agent, 自律エージェント |
| <a id="agentic-system"></a>Agentic System | エージェントシステム | エージェント、ワークフロー、ツール、コンテキスト、評価、ガードレールを組み合わせたAI実行システム全体。個々のエージェントではなく、運用可能な仕組み全体を指す。 | AIシステム, agentic system, agentic AI system, エージェント基盤 |
| <a id="workflow"></a>Workflow | ワークフロー | 開発者があらかじめ決めた手順や分岐に沿って、LLMやツールを組み合わせて実行する処理の流れ。自律的な判断よりも、設計された経路が中心になる。 | agent workflow, 処理フロー, 作業手順, 流れ |
| <a id="single-agent-system"></a>Single-Agent System | 単一エージェントシステム | ひとつのエージェントがツールやコンテキストを使ってタスクを進める構成。まずこの構成から始め、必要な場合だけマルチエージェント化する。 | single agent, monolithic agent, シンプル構成だけで説明すること |
| <a id="multi-agent-system"></a>Multi-Agent System | マルチエージェントシステム | 複数のエージェントが役割分担、委譲、協調、レビュー、統合を行いながらタスクを解く構成。単一エージェントより複雑になるため、必要性と評価が重要。 | multi-agent, 複数エージェント, agentic system と完全に同義にすること |
| <a id="agent-loop"></a>Agent Loop | エージェントループ | エージェントが観察、判断、ツール利用、結果確認、次の行動選択を繰り返す実行サイクル。長い作業や不確実な作業で重要になる。 | loop, 実行ループ, 作業ループ, reasoning loop |
| <a id="automation-level"></a>Automation Level | 自動化レベル | エージェントにどこまで自律的な判断・操作を任せるかの段階。読み取りのみ、提案のみ、承認付き実行、自律実行などで定義する。 | autonomy level と完全に同義にすること, 自動化率, 自律度だけで説明すること |
| <a id="advisory-mode"></a>Advisory Mode | 提案モード | エージェントが推奨案、下書き、判断材料、次アクションを提示するが、実行は人間が行う自動化レベル。 | advice mode, recommend-only, 提案だけ |
| <a id="approval-gated-execution"></a>Approval-Gated Execution | 承認付き実行 | エージェントが操作を準備・提案し、人間またはポリシーの承認後に実行する方式。高リスク操作や外部送信で使う。 | act with approval, approval mode, 人間確認付き実行 |
| <a id="autonomous-execution"></a>Autonomous Execution | 自律実行 | 事前に定めた権限、ガードレール、停止条件、監視の範囲内で、エージェントが人間承認なしに操作を実行する方式。 | full automation, 完全自動化, unattended execution |
| <a id="tool"></a>Tool | ツール | エージェントが外部情報を取得したり、環境に作用したりするために呼び出す機能。検索、ファイル操作、API呼び出し、コード実行などを含む。 | 外部ツール, function, API, 道具 |
| <a id="tool-use"></a>Tool Use | ツール利用 | エージェントが目的達成のためにツールを選び、入力を作り、結果を解釈して次の行動に反映すること。単なる関数呼び出しではなく、判断を含む行為を指す。 | tool calling, function calling, ツール呼び出し |
| <a id="tool-call"></a>Tool Call | ツール呼び出し | エージェントやモデルが、特定のツール名と入力を指定して実行を要求すること。Tool Use の中の実行単位として扱う。 | tool use と完全に同義にすること, function calling と完全に同義にすること |
| <a id="tool-definition"></a>Tool Definition | ツール定義 | エージェントがツールを正しく選択・実行するために渡す、ツール名、説明、入力スキーマ、返却値、使用条件などの定義。 | tool spec, function definition, API定義 |
| <a id="tool-description"></a>Tool Description | ツール説明 | ツールの目的、使うべき場面、使ってはいけない場面、入力の意味、返却値の読み方をエージェント向けに説明する文章。 | description, 説明文, API説明 |
| <a id="tool-result"></a>Tool Result | ツール結果 | ツール実行後にエージェントへ返す結果。成功/失敗だけでなく、次の判断に必要な情報、エラー理由、推奨アクションを含める。 | result, output, response, 返却値 |
| <a id="tool-surface"></a>Tool Surface | ツールサーフェス | エージェントに公開されているツール群、名前空間、引数、説明の全体。広すぎると選択ミスやコンテキスト消費が増えるため、絞り込みが重要になる。 | tool list, available tools, ツール一覧 |
| <a id="tool-permission"></a>Tool Permission | ツール権限 | ツールごとに許可される読み取り、書き込み、実行、送信、削除などの操作範囲。ガードレールや人間確認と組み合わせて管理する。 | permission, access, 権限, ツールアクセス |
| <a id="tool-discovery"></a>Tool Discovery | ツール発見 | エージェントやクライアントが、利用可能なツールの一覧、説明、入力スキーマ、使用条件を取得し、必要に応じて選べるようにすること。 | tool list と完全に同義にすること, retrieval と完全に同義にすること |
| <a id="agent-computer-interface"></a>Agent-Computer Interface | - | エージェントがコンピュータ、API、ファイル、アプリケーション、実行環境を扱うためのインターフェース設計。人間向けUI/APIではなく、エージェントが迷わず使える境界を設計する考え方。略称は ACI。 | ACIだけで初出すること, AI向けツール, human API |
| <a id="tool-wrapper"></a>Tool Wrapper | ツールラッパー | 既存APIや複数操作を、エージェントが目的単位で使いやすい形に包み直したツール。人間や通常プログラム向けAPIをそのまま渡さないために使う。 | wrapper, APIラップ, ラッパー |
| <a id="function-calling"></a>Function Calling | - | モデルが定義済みの関数形式ツールを選び、構造化された引数を生成して、外部機能やアプリケーション側の処理と接続する仕組み。OpenAI文脈では実装寄りの語として扱う。 | tool calling と完全に混同すること, 関数呼び出しだけで説明すること |
| <a id="hosted-tool"></a>Hosted Tool | - | モデル提供側やプラットフォーム側でホストされる検索、ファイル検索、コード実行などの組み込みツール。自前実装の負担を下げられるが、提供環境への依存がある。 | built-in tool, hosted tools, 組み込みツール |
| <a id="suggested-next-action"></a>Suggested Next Action | 推奨次アクション | ツール結果やツールエラーに含める、エージェントが次に取るべき行動の提案。再試行、入力修正、人間確認、別ツール利用、停止などを示す。 | recommendation と完全に同義にすること, error message と完全に同義にすること |
| <a id="context"></a>Context | コンテキスト | エージェントが次の判断や出力に使う情報の集合。システム指示、ユーザー入力、ツール定義、ツール結果、外部データ、会話履歴、作業状態などを含む。 | 文脈, prompt, 入力, 背景情報 |
| <a id="context-engineering"></a>Context Engineering | コンテキスト設計 | エージェントが適切に判断できるように、目的、背景、制約、関連情報、ツール結果、成功条件を設計して渡すこと。単なるプロンプト文面ではなく、利用可能な文脈全体を扱う。 | context engineering, 文脈設計, プロンプト設計 |
| <a id="context-state"></a>Context State | コンテキスト状態 | ある時点でエージェントに渡され、判断に影響しているコンテキスト全体の状態。単発プロンプトではなく、変化し続ける作業状態として扱う。 | state, 状態, working context, コンテキスト全体 |
| <a id="static-context"></a>Static Context | 静的コンテキスト | 実行中に基本的には変わらない指示や知識。システム指示、役割、ポリシー、固定ルール、リポジトリの恒久的な作法などを指す。 | 固定情報, static information, permanent context |
| <a id="dynamic-context"></a>Dynamic Context | 動的コンテキスト | 実行中に更新される情報。ユーザーの追加指示、ツール結果、現在の作業状況、途中経過、取得した外部情報などを指す。 | 動的情報, runtime context, current context |
| <a id="session-memory"></a>Session Memory | セッションメモリ | ひとつの会話や作業セッション内で、過去のやり取りや作業状態を保持する短期的なメモリ。複数ターンの一貫性を保つために使う。 | session, conversation memory, 会話履歴だけで説明すること |
| <a id="short-term-memory"></a>Short-Term Memory | 短期メモリ | 現在のスレッドやセッション内で有効なメモリ。直近の会話、途中状態、現在の目的、未完了タスクなどを保持する。 | STM, thread memory, working memory |
| <a id="long-term-memory"></a>Long-Term Memory | 長期メモリ | セッションをまたいで保持されるメモリ。ユーザー設定、継続的なプロジェクト知識、過去から再利用すべき事実などを扱う。 | LTM, persistent memory, 永続メモリ |
| <a id="context-compression"></a>Context Compression | コンテキスト圧縮 | 長くなった会話履歴や作業ログから重要情報を残し、不要情報を削って、短く有用な形にまとめ直すこと。 | compression, compaction, summarization, 要約 |
| <a id="context-trimming"></a>Context Trimming | コンテキストトリミング | 古い情報、低関連情報、不要な履歴をコンテキストから取り除き、現在の目的に必要な情報を残すこと。 | trimming, pruning, 削除, 履歴カット |
| <a id="retrieval"></a>Retrieval | 検索取得 | 必要なタイミングで外部データ、過去ログ、ドキュメント、長期メモリなどから関連情報を探してコンテキストに追加すること。 | retrieve, search, lookup, 検索だけで説明すること |
| <a id="prompt-chaining"></a>Prompt Chaining | プロンプトチェーン | 複雑なタスクを複数のLLM呼び出しに分け、前の出力を次の入力として渡しながら処理するワークフローパターン。各ステップを検査・調整しやすい。 | prompt chain, chain, チェーン, プロンプト連鎖 |
| <a id="routing"></a>Routing | ルーティング | 入力内容や状況に応じて、使用する処理、ツール、モデル、エージェントを選び分けるワークフローパターン。 | route, 分岐, 振り分け, intent routing |
| <a id="parallelization"></a>Parallelization | 並列化 | 複数の処理、LLM呼び出し、ツール実行、エージェント作業を並列に走らせ、速度向上や多面的な検討を行うワークフローパターン。 | parallel execution, 並列実行, parallel pattern |
| <a id="orchestrator-workers"></a>Orchestrator-Workers | オーケストレーター・ワーカー | 中央のオーケストレーターがタスクを分解し、複数のワーカーに割り当て、結果を統合するワークフローパターン。 | orchestrator-workers, coordinator-workers, supervisor-workers |
| <a id="evaluator-optimizer"></a>Evaluator-Optimizer | 評価者・改善者 | 生成結果を評価者が確認し、その評価をもとに改善者が出力を修正する反復型ワークフローパターン。品質改善ループに使う。 | evaluator optimizer, review loop, critique loop, 評価改善ループ |
| <a id="orchestration"></a>Orchestration | オーケストレーション | 複数のステップ、ツール、エージェント、分岐、ハンドオフをどの順番・条件で動かすかを設計し管理すること。 | orchestration, 流れの設計, 司令塔, 実行管理 |
| <a id="task-decomposition"></a>Task Decomposition | タスク分解 | 大きな目的や曖昧な依頼を、実行可能な小さな作業単位に分けること。ワークフロー設計やマルチエージェント設計の前提になる。 | decomposition, step decomposition, 作業分解, ステップ分解 |
| <a id="handoff"></a>Handoff | ハンドオフ | あるエージェントや処理ステップから、別の専門エージェントや担当処理へ作業を引き渡すこと。責任の移動と、渡す情報の設計が重要になる。 | handoff, 委譲, 引き継ぎ, agent handoff |
| <a id="delegation"></a>Delegation | 委譲 | あるエージェントやオーケストレーターが、特定の作業や判断を別のエージェントへ任せること。Handoff より一般的な作業割り当てとして使う。 | handoff と完全に同義にすること, assignment と完全に同義にすること |
| <a id="agent-as-tool"></a>Agent-as-Tool | ツールとしてのエージェント | 中央エージェントが他のエージェントをツールのように呼び出して、特定サブタスクの結果を得る構成。呼び出される側は独立した専門処理を担当する。 | agents as tools, tool agent, handoff と完全に同義にすること |
| <a id="result-synthesis"></a>Result Synthesis | 結果統合 | 複数エージェントや複数処理から得た結果を比較・要約・矛盾解消し、最終出力や次の判断にまとめること。 | synthesis, integration と完全に同義にすること, まとめだけで説明すること |
| <a id="supervisor-agent"></a>Supervisor Agent | スーパーバイザーエージェント | 複数の専門エージェントの選択、呼び出し、作業配分、結果統合を管理する上位エージェント。階層型マルチエージェント構成で使う。 | supervisor, lead agent, coordinator, manager agent |
| <a id="worker-agent"></a>Worker Agent | ワーカーエージェント | スーパーバイザーやオーケストレーターから割り当てられた特定タスクを実行する専門エージェント。 | worker, subagent, specialist agent, 専門エージェント |
| <a id="specialist-agent"></a>Specialist Agent | 専門エージェント | 特定の領域、作業、判断、ツール利用に特化したエージェント。調査、要約、コード、レビュー、財務分析などの役割を持つ。 | worker agent と完全に同義にすること, subagent と完全に同義にすること |
| <a id="sub-agent"></a>Sub-Agent | サブエージェント | あるエージェントやオーケストレーターから呼び出される下位エージェント。専門性を持つ場合もあるが、階層上の位置を表す語として使う。 | specialist agent と完全に同義にすること, child agent, worker と完全に同義にすること |
| <a id="agent-team"></a>Agent Team | エージェントチーム | 共通の目的に対して、複数の専門エージェントやサブエージェントを組み合わせたチーム構成。役割、責任、通信経路、統合方法を明確にする。 | multi-agent system と完全に同義にすること, teamだけで説明すること |
| <a id="shared-state"></a>Shared State | 共有状態 | 複数エージェントが参照・更新する共通の作業状態。進捗、メモ、部分結果、判断履歴などを含み、競合や汚染を防ぐ設計が必要。 | shared memory と完全に同義にすること, state と完全に同義にすること |
| <a id="communication-flow"></a>Communication Flow | 通信フロー | エージェント間で、依頼、結果、状態、判断、エラー、承認要求などがどの順番・経路で渡るかを示す流れ。 | message flow と完全に同義にすること, orchestration と完全に同義にすること |
| <a id="evaluation"></a>Evaluation | 評価 | エージェントやワークフローが期待どおりに動いているかを、成功条件、評価ケース、ログ、トレース、人間評価などで確認すること。開発後だけでなく改善サイクルの一部として扱う。 | eval, evals, evaluation, テスト |
| <a id="eval"></a>Eval | 評価ケース | エージェントやワークフローの特定の振る舞いを確認するための評価単位。入力、期待される振る舞い、採点方法、合否基準などを含む。 | evaluation と同義にすること, test case, テストケース |
| <a id="eval-suite"></a>Eval Suite | 評価スイート | 複数の評価ケースを目的別にまとめた評価セット。品質確認、回帰確認、安全性確認など、用途ごとに分けて運用する。 | evals, evaluation set, test suite |
| <a id="grader"></a>Grader | 採点器 | 評価対象の出力や行動を、ルール、人間、LLM、プログラムなどで採点する仕組み。合否だけでなくスコアや理由を返すこともある。 | evaluator, judge, LLM judge, 採点者 |
| <a id="model-based-grading"></a>Model-Based Grading | モデル採点 | LLMなどのモデルを採点器として使い、出力の品質、正確性、根拠性、安全性などを評価する方法。人間採点の代替または補助として使う。 | LLM judge と完全に同義にすること, grader と完全に同義にすること |
| <a id="evaluation-harness"></a>Evaluation Harness | 評価ハーネス | 評価対象の実行、データセットの投入、採点器の適用、結果集計、トレース保存をまとめて行う評価実行基盤。評価の再現性と継続運用を支える。 | eval harness, test runner と完全に同義にすること |
| <a id="regression-testing"></a>Regression Testing | 回帰テスト | プロンプト、モデル、ツール、ワークフロー変更後に、以前できていた振る舞いが壊れていないか確認する評価。 | regression eval, 回帰評価, リグレッションテスト |
| <a id="trace"></a>Trace | トレース | 1回のエージェント実行で起きたモデル呼び出し、ツール利用、ハンドオフ、ガードレール、カスタムイベントなどの時系列記録。 | log, 実行履歴, tracing と混同すること |
| <a id="span"></a>Span | スパン | トレース内のひとつの処理区間。モデル呼び出し、ツール実行、ハンドオフなど、開始と終了を持つ単位として記録される。 | event, log entry, step |
| <a id="observability"></a>Observability | 観測可能性 | エージェントやLLMアプリケーションの内部動作を、トレース、ログ、メトリクス、評価結果などから理解・調査できる状態や仕組み。 | monitoring と同義にすること, 可観測性, 監視 |
| <a id="feedback-loop"></a>Feedback Loop | フィードバックループ | エージェントの出力、実行結果、評価、ユーザーフィードバック、運用データを次の改善に戻す仕組み。改善サイクルを継続させるために使う。 | iteration と完全に同義にすること, review loop と完全に同義にすること |
| <a id="evaluation-drift"></a>Evaluation Drift | 評価ドリフト | モデル、データ、ユーザー入力、ツール、プロンプト、業務条件の変化により、過去の評価結果が現在の性能やリスクを表しにくくなること。 | data drift と完全に同義にすること, quality degradation と完全に同義にすること |
| <a id="guardrail"></a>Guardrail | ガードレール | 入力、出力、ツール利用、権限操作などを検査・制御し、エージェントの危険な振る舞いを防ぐための安全制御。 | guardrails, 入力制御, 出力制御, 安全策 |
| <a id="input-guardrail"></a>Input Guardrail | 入力ガードレール | ユーザー入力やツール実行前の入力を検査し、危険・不正・不適切な内容を検出した場合に、拒否、修正、停止、エスカレーションへ移す制御。 | input validation と完全に同義にすること, moderation と完全に同義にすること |
| <a id="output-guardrail"></a>Output Guardrail | 出力ガードレール | モデル出力、ツール結果、外部送信前の内容を検査し、不適切な出力、機密情報、危険操作、形式違反を防ぐ制御。 | output validation と完全に同義にすること, post-processing と完全に同義にすること |
| <a id="tripwire"></a>Tripwire | トリップワイヤー | ガードレールが重大な条件違反や危険を検出したときに発火し、実行停止、一時停止、拒否応答、人間レビューなどへ切り替える仕組み。 | alert と完全に同義にすること, stop condition と完全に同義にすること |
| <a id="human-review"></a>Human Review | 人間レビュー | エージェントの出力、判断、ツール利用、外部送信、高リスク操作などを人間が確認すること。完全自動化せず、人間の判断を挟む安全制御として使う。 | human-in-the-loop, 人間確認, 確認, review |
| <a id="approval"></a>Approval | 承認 | エージェントが特定の操作を実行する前に、人間や管理者が明示的に許可すること。特に送信、削除、購入、公開、本番操作などで使う。 | approve, permission, 許可, 確認 |
| <a id="checkpoint"></a>Checkpoint | 確認ポイント | ワークフローやエージェントループの途中で、人間レビュー、承認、状態確認、方針変更を行うために設ける停止地点。 | checkpoint, approval point, 確認地点 |
| <a id="escalation"></a>Escalation | エスカレーション | エージェントが自力で判断・処理すべきでない状況を、人間、管理者、専門担当、別システムに引き上げること。 | escalate, 引き上げ, 相談, 人に回す |
| <a id="stop-condition"></a>Stop Condition | 停止条件 | エージェント実行を続けず、停止・拒否・エスカレーション・人間確認に移るべき条件。危険操作、権限不足、不確実性、失敗回数などで定義する。 | checkpoint と完全に同義にすること, end condition, 終了条件 |
| <a id="safe-failure"></a>Safe Failure | 安全な失敗 | 不確実性、エラー、権限不足、危険操作、判断不能が起きたときに、被害を広げず、停止・確認・エスカレーションできる失敗の仕方。 | fail-safe と完全に同義にすること, ただ停止すること |
| <a id="refusal"></a>Refusal | 拒否応答 | 安全上・権限上・仕様上・不確実性上の理由で、モデルやエージェントが依頼を実行しない、または一部だけ実行する応答。理由と安全な代替案を伴うことが多い。 | error と完全に同義にすること, failure と完全に同義にすること, 断るだけで説明すること |
| <a id="prompt-injection"></a>Prompt Injection | プロンプトインジェクション | ユーザー入力、外部文書、Webページ、ツール結果などに紛れた悪意ある指示によって、エージェントの判断や出力、ツール利用を意図しない方向へ操作する攻撃。 | injection, プロンプト攻撃, 脱獄と同義にすること |
| <a id="indirect-prompt-injection"></a>Indirect Prompt Injection | 間接プロンプトインジェクション | エージェントが読む外部文書、Webページ、メール、チケット、ツール結果などを経由して混入するプロンプトインジェクション。ユーザーが直接入力していない点が特徴。 | indirect injection, 外部文書攻撃, prompt injection と完全に同義にすること |
| <a id="jailbreak"></a>Jailbreak | 脱獄 | モデルやエージェントに設定された安全制約、ポリシー、出力制限を回避させようとする攻撃や入力。Prompt Injection とは重なるが、主に制約回避に焦点がある。 | prompt injection と完全に同義にすること, 制限解除 |
| <a id="untrusted-content"></a>Untrusted Content | 信頼しないコンテンツ | 外部文書、Webページ、メール、ユーザー投稿、ツール結果など、内部指示として扱ってはいけない入力。内容は参照できるが、命令としては信頼しない。 | external content, 外部コンテンツ, 信頼できない情報 |
| <a id="sensitive-information"></a>Sensitive Information | 機密情報 | 漏洩・不正利用・不適切な共有によって個人、顧客、組織、事業に損害を与え得る情報。個人情報、認証情報、契約情報、社内機密などを含む。 | confidential information, secret, 秘密情報, 個人情報だけで説明すること |
| <a id="credential"></a>Credential | 認証情報 | APIキー、アクセストークン、パスワード、SSHキー、Cookie など、システムやサービスへのアクセスに使われる情報。機密情報の中でも特に厳格に扱う。 | credentials, secret, token, password と同義にすること |
| <a id="data-exfiltration"></a>Data Exfiltration | データ持ち出し | 機密情報、個人データ、社内資料、コード、ログなどが、意図しない外部宛先や許可されていない場所へ送られること。 | exfiltration, leak, 情報漏洩と完全に同義にすること |
| <a id="tool-poisoning"></a>Tool Poisoning | ツールポイズニング | ツール名、ツール説明、入力スキーマ、MCPサーバーのメタデータなどに悪意ある指示や誤誘導を混ぜ、エージェントのツール選択や実行を操作する攻撃。 | tool injection, poisoned tool, prompt injection と完全に同義にすること |
| <a id="excessive-agency"></a>Excessive Agency | 過剰な自律性 | エージェントに必要以上の権限、自由度、外部操作能力、長時間実行能力を与えた結果、意図しない操作や被害拡大につながるリスク。 | excessive autonomy, 過剰権限だけで説明すること |
| <a id="overreliance"></a>Overreliance | 過信 | エージェントやLLMの出力・判断を、人間が十分に検証せず信頼しすぎること。誤回答、誤判断、専門家確認漏れ、承認の形骸化につながる。 | automation bias と完全に同義にすること, 盲信 |
| <a id="retrieval-augmented-generation"></a>Retrieval-Augmented Generation | 検索拡張生成 | 外部ドキュメントやナレッジベースから関連情報を検索取得し、その内容を根拠としてモデルに回答を生成させる構成。略称は RAG。 | RAGだけで初出すること, retrieval と完全に同義にすること, 検索だけで説明すること |
| <a id="knowledge-base"></a>Knowledge Base | ナレッジベース | エージェントやLLMが検索取得して参照できるように整備された知識集合。ドキュメント、FAQ、仕様書、ログ、社内資料などを含み、回答や判断の根拠として使う。 | knowledge, docs, document store と完全に同義にすること |
| <a id="chunk"></a>Chunk | チャンク | 検索や埋め込みのために、ドキュメントを一定の意味やサイズで分割した単位。検索精度や回答品質に大きく影響する。 | section と完全に同義にすること, paragraph と完全に同義にすること |
| <a id="embedding"></a>Embedding | 埋め込み | テキストや画像などをベクトル表現に変換したもの。意味的に近い情報を検索する semantic search や vector search に使う。 | vector と完全に同義にすること, ベクトル化だけで説明すること |
| <a id="vector-store"></a>Vector Store | ベクトルストア | Embedding と対応するテキスト・メタデータを保存し、意味検索や類似検索に使うデータストア。RAG や File Search の基盤になる。 | vector database と完全に同義にすること, knowledge base と完全に同義にすること |
| <a id="semantic-search"></a>Semantic Search | 意味検索 | キーワード一致だけでなく、Embedding などを使って意味的に近い情報を検索する方法。表現揺れや類似概念の検索に強い。 | vector search と完全に同義にすること, 類似検索だけで説明すること |
| <a id="hybrid-search"></a>Hybrid Search | ハイブリッド検索 | Keyword Search と Semantic Search / Vector Search など複数の検索方法を組み合わせる検索方式。正確一致と意味的類似の両方を活かす。 | semantic search と完全に同義にすること, keyword search と完全に同義にすること |
| <a id="retriever"></a>Retriever | リトリーバー | クエリに対して関連するドキュメント、チャンク、リソースを取得する部品。検索方式、フィルタ、ランキング、権限制御などを含むことがある。 | retrieval と完全に同義にすること, search engine と完全に同義にすること |
| <a id="reranking"></a>Reranking | 再ランキング | 検索で取得した候補を、関連性、信頼性、回答への有用性などに基づいて並べ替える処理。回答品質や引用精度に影響する。 | ranking と完全に同義にすること, sortingだけで説明すること |
| <a id="grounding"></a>Grounding | グラウンディング | モデルの回答や判断を、検索取得したソース、ツール結果、データベース、引用可能な根拠に接続すること。幻覚を減らし、検証可能性を高める。 | citation と完全に同義にすること, retrieval と完全に同義にすること |
| <a id="evidence"></a>Evidence | 根拠 | 回答、判断、評価、承認の裏付けとして使う具体的な情報。引用箇所、検索結果、ツール結果、ログ、評価結果などを含む。 | source と完全に同義にすること, citation と完全に同義にすること |
| <a id="citation"></a>Citation | 引用 | 回答や説明の根拠として、どの情報源・箇所に基づいたかを示す表示。ユーザーやレビュアーが根拠を確認できるようにする。 | source と完全に同義にすること, link だけで説明すること |
| <a id="query"></a>Query | クエリ | 検索取得のために入力される問い合わせ。ユーザー文そのものとは限らず、エージェントが目的に合わせて書き換えたり分解したりすることがある。 | user request と完全に同義にすること, prompt と完全に同義にすること |
| <a id="search-relevance"></a>Search Relevance | 検索関連性 | 検索結果がクエリやタスク目的にどれだけ合っているか。意味的近さ、キーワード一致、権限、鮮度、信頼性、再ランキングなどで改善する。 | relevance だけで説明すること, accuracy と完全に同義にすること |
| <a id="search-coverage"></a>Search Coverage | 検索カバレッジ | 検索対象の中から、必要な情報をどれだけ漏れなく拾えるか。Chunking、Metadata、Query、Hybrid Search、Retriever 設計に影響される。 | recall と完全に同義にすること, coverage だけで説明すること |
| <a id="large-language-model"></a>Large Language Model | 大規模言語モデル | 自然言語やコードなどの入力を処理し、テキスト生成、推論、分類、ツール利用の判断などを行う基盤モデル。略称は LLM。 | LLMだけで初出すること, AIモデル, モデルだけで説明すること |
| <a id="reasoning-model"></a>Reasoning Model | 推論モデル | 複雑な問題解決、計画、コード、数学、多段推論などに強く、通常の応答生成より深い推論過程を使うモデル。 | reasoning, 高性能モデル, thinking model |
| <a id="model-selection"></a>Model Selection | モデル選定 | 品質、速度、コスト、コンテキスト長、ツール利用能力、安全要件などに応じて、利用するモデルを選ぶこと。 | model choice, モデル選択, とりあえず高性能モデルを使うこと |
| <a id="structured-output"></a>Structured Output | 構造化出力 | モデルの出力を JSON schema などの決まった構造に従わせ、アプリケーション側で扱いやすくする仕組み。評価、ツール連携、後続処理に使う。 | JSON output, format指定, structured outputs |
| <a id="nondeterminism"></a>Nondeterminism | 非決定性 | 同じ入力や近い条件でも、モデル出力やエージェント行動が完全には同一にならない性質。サンプリング、モデル更新、ツール結果、外部状態などが原因になる。 | randomness と完全に同義にすること, 不安定性だけで説明すること |
| <a id="reproducibility"></a>Reproducibility | 再現性 | 同じ条件や記録された手順に基づいて、同等の結果や評価を再現できる性質。モデル、プロンプト、パラメータ、データ、環境の記録が重要になる。 | determinism と完全に同義にすること, repeatability と完全に同義にすること |
| <a id="temperature"></a>Temperature | 温度 | モデル出力のランダム性や多様性に影響する生成パラメータ。低いほど一貫しやすく、高いほど多様になりやすい。 | creativity と完全に同義にすること, randomness と完全に同義にすること |
| <a id="top-p"></a>Top-P | - | 確率の高い候補トークンを累積確率のしきい値で絞り込む生成パラメータ。出力の多様性や予測可能性を調整する。 | nucleus samplingだけで説明すること, temperature と完全に同義にすること |
| <a id="seed"></a>Seed | シード | 生成時の乱数初期値。固定すると同じ応答に近づけられる場合があるが、確定的出力を保証するものではない。 | reproducibility と完全に同義にすること, random seedだけで説明すること |
| <a id="model-version"></a>Model Version | モデルバージョン | 使用するモデルの具体的な版や識別子。評価結果、再現性、品質比較、ロールバック、本番利用判断で記録する必要がある。 | model と完全に同義にすること, snapshot と完全に同義にすること |
| <a id="hallucination-rate"></a>Hallucination Rate | ハルシネーション率 | 出力のうち、根拠のない事実、誤った引用、存在しない情報、ソースと矛盾する主張が含まれる割合。Grounding や Citation の品質改善に使う。 | error rate と完全に同義にすること, 幻覚率だけで説明すること |
| <a id="computer-use"></a>Computer Use | コンピューター操作 | エージェントがスクリーンショットを見ながら、クリック、入力、スクロール、キー操作などでコンピューター環境を操作する能力や仕組み。 | computer tool と完全に同義にすること, ブラウザ操作だけで説明すること |
| <a id="computer-tool"></a>Computer Tool | コンピューターツール | Computer Use を実現するために、スクリーンショット取得やマウス・キーボード操作を提供するツール。実装によってブラウザ、VM、デスクトップ環境を操作する。 | computer use と完全に同義にすること, UI tool, automation tool |
| <a id="gui-agent"></a>GUI Agent | GUIエージェント | 画面上のUIを認識し、クリック、入力、スクロール、タップなどを通じてアプリケーションを操作するエージェント。構造化APIではなく視覚的UIを主な操作対象にする。 | browser agent と完全に同義にすること, computer use と完全に同義にすること |
| <a id="browser-agent"></a>Browser Agent | ブラウザエージェント | Webブラウザ上でページ閲覧、フォーム入力、クリック、検索、予約、注文などを行うエージェント。GUI Agent の一種として扱う。 | GUI agent と完全に同義にすること, web crawler と完全に同義にすること |
| <a id="screenshot"></a>Screenshot | スクリーンショット | エージェントが現在の画面状態を理解するために取得する画像。外部ページやメールなどが写る場合は、信頼しないコンテンツとして扱う。 | screen capture, 画像だけで説明すること |
| <a id="ui-action"></a>UI Action | UI操作 | エージェントが画面上で行うクリック、入力、スクロール、キー操作、ドラッグ、タップなどの操作単位。高リスク操作では承認や停止条件と結びつける。 | action と完全に同義にすること, tool call と完全に同義にすること |
| <a id="browser-session"></a>Browser Session | ブラウザセッション | ブラウザ操作中に保持されるログイン状態、Cookie、タブ、履歴、ページ状態などを含む作業状態。権限や個人データに直結するため管理対象にする。 | session memory と完全に同義にすること, browser state と完全に同義にすること |
| <a id="visual-grounding"></a>Visual Grounding | 視覚的グラウンディング | エージェントがスクリーンショットや画面要素を根拠に、どこを操作すべきか、何が表示されているかを判断すること。誤認識や視覚的誘導のリスクがある。 | grounding と完全に同義にすること, image recognition だけで説明すること |
| <a id="model-context-protocol"></a>Model Context Protocol | - | LLMアプリケーションが外部ツールやデータソースと接続するための標準プロトコル。略称は MCP。用語集では初出のみ正式名を使い、以後は MCP として扱う。 | MCPだけで初出すること, model context protocol, 外部ツール規格 |
| <a id="connector"></a>Connector | コネクタ | エージェントやLLMアプリケーションが、外部サービス、データベース、業務アプリ、ファイルストアなどへ接続するための部品や接続口。 | integration と完全に同義にすること, plugin と完全に同義にすること |
| <a id="resource"></a>Resource | リソース | MCP などで、LLMやエージェントに context を提供するために公開されるデータ単位。ファイル、DBスキーマ、アプリ固有情報などを含み、URIで識別される。 | file と完全に同義にすること, tool と混同すること |
| <a id="capability"></a>Capability | ケイパビリティ | サーバー、ツール、エージェント、モデル、実行環境が提供できる機能や能力。利用可能な操作範囲や対応機能を示す。 | feature と完全に同義にすること, permission と完全に同義にすること |
| <a id="interoperability"></a>Interoperability | 相互運用性 | 異なるモデル、エージェント、ツール、データソース、プラットフォームが、共通仕様や接続方式を通じて連携できる性質。 | compatibility と完全に同義にすること, integration と完全に同義にすること |
| <a id="vendor-lock-in"></a>Vendor Lock-In | ベンダーロックイン | 特定のベンダー、モデル、SDK、ツール、データ基盤に依存しすぎて、他環境への移行や併用が難しくなる状態。 | dependency と完全に同義にすること, platform risk と完全に同義にすること |
